// SHE SAFE frontend config
//
// Frontend and backend run as two separate processes/servers.
// Change this one line if your backend runs somewhere else
// (a different port, another machine on the LAN, a deployed URL, etc).
// admin-login.js and admin-dashboard.js both read window.SHE_SAFE_API_BASE.

window.SHE_SAFE_API_BASE = "https://she-safe-backend.onrender.com/api";
